apiVersion: apps/v1
kind: Deployment
metadata:
  name: splunk-ao-banking-qwen
  namespace: ${ACK_NAMESPACE}
  labels:
    app.kubernetes.io/name: splunk-ao-banking-qwen
    app.kubernetes.io/part-of: splunk-ao-banking-qwen-demo
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app.kubernetes.io/name: splunk-ao-banking-qwen
  template:
    metadata:
      labels:
        app.kubernetes.io/name: splunk-ao-banking-qwen
        app.kubernetes.io/part-of: splunk-ao-banking-qwen-demo
    spec:
      automountServiceAccountToken: false
      imagePullSecrets:
        - name: registry-pull
      securityContext:
        runAsNonRoot: true
        runAsUser: 999
        runAsGroup: 999
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: banking-chatbot
          image: ${IMAGE_REF}
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          envFrom:
            - configMapRef:
                name: splunk-ao-banking-qwen-config
            - secretRef:
                name: splunk-ao-banking-qwen-runtime
          env:
            # Projected ConfigMap files update in-place. New Chainlit sessions read
            # these paths, so prompt A/B switches do not restart the Pod.
            - name: SUPERVISOR_PROMPT_PROFILE_FILE
              value: /etc/banking-prompt/SUPERVISOR_PROMPT_PROFILE
            - name: SUPERVISOR_PROMPT_CUSTOM_FILE
              value: /etc/banking-prompt/SUPERVISOR_PROMPT_CUSTOM
          volumeMounts:
            - name: supervisor-prompt
              mountPath: /etc/banking-prompt
              readOnly: true
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 2Gi
          startupProbe:
            tcpSocket:
              port: http
            periodSeconds: 5
            failureThreshold: 36
          readinessProbe:
            tcpSocket:
              port: http
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: http
            periodSeconds: 20
            timeoutSeconds: 2
            failureThreshold: 3
          terminationMessagePolicy: FallbackToLogsOnError
      volumes:
        - name: supervisor-prompt
          configMap:
            name: splunk-ao-banking-qwen-config
            items:
              - key: SUPERVISOR_PROMPT_PROFILE
                path: SUPERVISOR_PROMPT_PROFILE
              - key: SUPERVISOR_PROMPT_CUSTOM
                path: SUPERVISOR_PROMPT_CUSTOM
      terminationGracePeriodSeconds: 30
