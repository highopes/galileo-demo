apiVersion: v1
kind: Service
metadata:
  name: splunk-ao-banking-qwen
  namespace: ${ACK_NAMESPACE}
  labels:
    app.kubernetes.io/name: splunk-ao-banking-qwen
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: splunk-ao-banking-qwen
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
